<tool id="humann2_join_tables" name="Join HUMAnN2 table" version="0.6.1">
    <description></description>

    <macros>
        <import>humann2_macros.xml</import>
    </macros>

    <expand macro="requirements"/>
    <expand macro="stdio"/>

    <version_command>
<![CDATA[
    humann2_join_tables --version
]]>
    </version_command>

    <command><![CDATA[
        Rscript $__tool_directory__/plot_grouped_barplot.R
            `mkdir tmp_dir`
            && 

            #for $file in $files:
                cp $file.input_file tmp_dir/\${file.sample_name}           
            #end for

            &&

            humann2_split_table
                #for $file in $files:
                    --names ${file.sample_name}
                    -i "input_files"  
                #end for

            -o $joined_table           

    ]]></command>

    <inputs>

        <repeat name="files" title="Add an input file" >
            <param name="sample_name" type="text" label="Name of the file" help="(--sample_name)"/>
            <param name="input_files" type="data" format="tsv,biom" label="Gene/pathway tables" help=""/>
        </repeat>
    </inputs>

    <outputs>
        <data format="tsv,biom" name="joined_table" label="Joined table for ${on_string} (HUMAnN2)" />
    </outputs>

    <tests>
        <test>
            <param name="files_0|sample_name" value="test_abun"/>
            <param name="files_0|input_files" value="expected_pathway_abundance.tsv"/>
            <param name="files_1|sample_name" value="test_cov"/>
            <param name="files_1|input_files" value="expected_pathway_coverage.tsv"/>
            <output name="joined_table" file="join_table_output.tsv,biom"/>
        </test>
    </tests>

    <help><![CDATA[
**What it does**

Join HUMAnN2 tables is a tool to join gene, pathway, or taxonomy tables of multiple samples in a single table.

HUMAnN is a pipeline for efficiently and accuretly profiling the presence/absence and abundance of microbial pathways in a community from metagenomic or metatranscriptomic sequencing data. `Read more about the tool <http://huttenhower.sph.harvard.edu/humann2/manual>`_.
    ]]></help>

    <citations>
    </citations>
</tool>