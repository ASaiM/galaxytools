<tool id="humann2_split_table" name="Split HUMAnN2 table" version="0.6.1">
    <description></description>

    <macros>
        <import>humann2_macros.xml</import>
    </macros>

    <expand macro="requirements"/>
    <expand macro="stdio"/>

    <version_command>
<![CDATA[
    humann2_split_table --version
]]>
    </version_command>

    <command><![CDATA[
        humann2_split_table
            -i $input_file
            -o "output"    

    ]]></command>

    <inputs>
        <param name="input_file" type="data" format="tsv,biom" multiple="true" 
            label="Gene/pathway table to read" help="use afile from join_table"/>
        </param>
    </inputs>

    <outputs>
        <collection type="list" name="split_tables" format="tsv">
            <discover_datasets pattern="__designation_and_ext__" directory="split" visible="true" />
        </collection>
    </outputs>

    <tests>
        <test>
            <param name="input_file" value="humann2_split_input_from_join.tsv,biom"/>
            <output name="report" file="humann2_split_1_output.tsv,humann2_split_2_output.tsv" />
        </test>
    </tests>

    <help><![CDATA[
**What it does**

Split HUMAnN2 table is a tool to split a gene/pathway table in table per sample.

HUMAnN is a pipeline for efficiently and accuretly profiling the presence/absence and abundance of microbial pathways in a community from metagenomic or metatranscriptomic sequencing data. `Read more about the tool <http://huttenhower.sph.harvard.edu/humann2/manual>`_.
    ]]></help>

    <citations>
    </citations>
</tool>